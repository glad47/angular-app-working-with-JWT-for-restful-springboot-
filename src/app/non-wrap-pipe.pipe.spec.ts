import { NonWrapPipePipe } from './non-wrap-pipe.pipe';

describe('NonWrapPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NonWrapPipePipe();
    expect(pipe).toBeTruthy();
  });
});
